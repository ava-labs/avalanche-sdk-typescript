/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { notificationsSubscriptions } from "../../funcs/notificationsSubscriptions.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.SubscriptionsRequest$inboundSchema,
};

export const tool$notificationsSubscriptions: ToolDefinition<typeof args> = {
  name: "notifications-subscriptions",
  description: `Get Subscriptions

Get subscriptions.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await notificationsSubscriptions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { notificationsUnsubscribe } from "../../funcs/notificationsUnsubscribe.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.UnsubscribeRequest$inboundSchema,
};

export const tool$notificationsUnsubscribe: ToolDefinition<typeof args> = {
  name: "notifications-unsubscribe",
  description: `Unsubscribe from Notifications

Unsubscribe from receiving notifications.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await notificationsUnsubscribe(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { notificationsAccess } from "../../funcs/notificationsAccess.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.AccessRequest$inboundSchema,
};

export const tool$notificationsAccess: ToolDefinition<typeof args> = {
  name: "notifications-access",
  description: `Access Notifications

Access notifications.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await notificationsAccess(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

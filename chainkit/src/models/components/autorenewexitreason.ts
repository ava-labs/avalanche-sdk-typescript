/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { ClosedEnum } from "../../types/enums.js";

/**
 * Why the validation ended. `graceful` = exited after being configured to stop renewing; `uptime_not_met` = removed because reward eligibility was not met. Null while still active (`renewing` or `exiting`). `state` carries the same distinction (`exited` vs `aborted`).
 */
export const AutoRenewExitReason = {
  Graceful: "graceful",
  UptimeNotMet: "uptime_not_met",
} as const;
/**
 * Why the validation ended. `graceful` = exited after being configured to stop renewing; `uptime_not_met` = removed because reward eligibility was not met. Null while still active (`renewing` or `exiting`). `state` carries the same distinction (`exited` vs `aborted`).
 */
export type AutoRenewExitReason = ClosedEnum<typeof AutoRenewExitReason>;

/** @internal */
export const AutoRenewExitReason$inboundSchema: z.ZodNativeEnum<
  typeof AutoRenewExitReason
> = z.nativeEnum(AutoRenewExitReason);
/** @internal */
export const AutoRenewExitReason$outboundSchema: z.ZodNativeEnum<
  typeof AutoRenewExitReason
> = AutoRenewExitReason$inboundSchema;

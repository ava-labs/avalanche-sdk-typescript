/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkUtxosListByAddressesV2 } from "../../funcs/dataPrimaryNetworkUtxosListByAddressesV2.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUtxosByAddressesV2Request$inboundSchema,
};

export const tool$dataPrimaryNetworkUtxosListByAddressesV2: ToolDefinition<
  typeof args
> = {
  name: "data-primary-network-utxos-list-by-addresses-v2",
  description: `List UTXOs v2 - Supports querying for more addresses

Lists UTXOs on one of the Primary Network chains for the supplied addresses. This v2 route supports increased page size and address limit.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataPrimaryNetworkUtxosListByAddressesV2(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};

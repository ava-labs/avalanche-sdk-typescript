/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsLiveCheck } from "../../funcs/metricsLiveCheck.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$metricsLiveCheck: ToolDefinition = {
  name: "metrics-live-check",
  description: `Get the liveliness of the service

Check the liveliness of the service.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await metricsLiveCheck(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

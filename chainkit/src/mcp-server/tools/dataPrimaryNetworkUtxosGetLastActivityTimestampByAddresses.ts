/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkUtxosGetLastActivityTimestampByAddresses } from "../../funcs/dataPrimaryNetworkUtxosGetLastActivityTimestampByAddresses.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetLastActivityTimestampByAddressesRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkUtxosGetLastActivityTimestampByAddresses:
  ToolDefinition<typeof args> = {
    name: "data-primary-network-utxos-get-last-activity-timestamp-by-addresses",
    description: `Get last activity timestamp by addresses

Gets the last activity timestamp for the supplied addresses on one of the Primary Network chains.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await dataPrimaryNetworkUtxosGetLastActivityTimestampByAddresses(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

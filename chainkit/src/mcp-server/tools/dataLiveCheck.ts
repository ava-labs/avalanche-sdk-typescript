/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataLiveCheck } from "../../funcs/dataLiveCheck.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$dataLiveCheck: ToolDefinition = {
  name: "data-live-check",
  description: `Get the liveliness of the service (reads only)

Check the liveliness of the service (reads only).`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await dataLiveCheck(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksGetAddressesFromWebhook } from "../../funcs/webhooksGetAddressesFromWebhook.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAddressesFromWebhookRequest$inboundSchema,
};

export const tool$webhooksGetAddressesFromWebhook: ToolDefinition<typeof args> =
  {
    name: "webhooks-get-addresses-from-webhook",
    description: `List adresses by EVM activity webhooks

List adresses by webhook. Only valid for EVM activity webhooks.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await webhooksGetAddressesFromWebhook(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksRemoveAddressesFromWebhook } from "../../funcs/webhooksRemoveAddressesFromWebhook.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAddressesFromWebhookRequest$inboundSchema,
};

export const tool$webhooksRemoveAddressesFromWebhook: ToolDefinition<
  typeof args
> = {
  name: "webhooks-remove-addresses-from-webhook",
  description: `Remove addresses from EVM activity  webhook

Remove addresses from webhook. Only valid for EVM activity webhooks.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksRemoveAddressesFromWebhook(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
